<nav>
	<ul>
		<li><%= link_to t('layout.new_room'), new_room_path %></li>
		<li><%= link_to t('layout.rooms') , rooms_path%></li>
		<li>
			<%= link_to t('layout.my_profile'), user_path(current_user) %>
		</li>
		<li>
			<%= link_to t('layout.myprofile'), user_path(current_user) %>
		</li>
		<li>
			<%= link_to t('layout.signout'), user_sessions_path(current_user), method: :delete %>
			<a href="/pt/user_sessions" data-method="delete" rel="nofollow"></a>
		</li>
	</ul>
</nav>